int main() {
    int suma = 0;
    const int limite = 10;

    for (int i = 0; i < 20; i++) {
        if (i == limite) {
            break;
        }
        if (i % 2 == 0) {
            printf("par: %d\n", i);
            suma = suma + i;
        }
    }

    printf("suma total: %d\n", suma);
}
