int main() {
    int x = 0;
    int y = 0;
    do {
        x = x + 1;
        y = y + 1;
        printf("x = %d\n", x);
        printf("y = %d\n", y);

        if (y == 1)
        {
		printf("wacho y es 1!!");
	    }    

    } while (x < 3);

	printf("hola");

}
